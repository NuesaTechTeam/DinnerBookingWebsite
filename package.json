{
  "name": "dinnerbookingwebsite",
  "version": "1.0.0",
  "private": true,
  "description": "Dinner Booking Website - frontend + backend",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "dev": "npm --prefix frontend run dev"
  },
  "license": "ISC"
}